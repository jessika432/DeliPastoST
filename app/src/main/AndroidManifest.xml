<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.delipasto">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".selecciondeusuario"></activity>
        <activity android:name=".InicioDeliPasto" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".historias" />
        <activity android:name=".abrirreceta" />
        <activity android:name=".BienvenidosTodos" />
        <activity android:name=".iniciodelareceta" />
        <activity android:name=".editar" />
        <activity android:name=".registrar" />
    </application>

</manifest>